<!DOCTYPE html>
<html lang="en">
<head>
    <title> <%= title %></title>
    <script src="https://kit.fontawesome.com/e57ed3e252.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1 id="page-heading">
        <i class="fas fa-address-book"></i>
        My Contact List</h1>
    <div class="container">
        <ul>
            <% for(let i of contact_list) {%>
                <li class="contact">
                    <p>Name  - <%= i.name %></p>
                    <p>Phone - <%= i.phone %></p>
                    <p>Email - <%= i.email %></p>
                    <p>
                        <span title="Delete Contact" class="delete-button">
                            <a href="/delete-contact/?email=<%= i.email %>">
                                <i class="fas fa-window-close"></i>
                            </a>
                        </span>
                        <span title="Update Contact" class="update-button">
                            <a href="/update-contact-form/?email=<%= i.email %>">
                                <i class="fas fa-pen"></i>
                            </a>
                        </span>
                    </p>
                </li>
            <%}%>
        </ul>
    </div>
    <div class="container">
        <form action="/create-contact" method="POST">
            <input type="text" name="name" placeholder="Enter Name">
            <input type="text" name="phone" placeholder="Enter Phone">
            <input type="email" name="email" placeholder="Enter Email">
            <button type="submit">Add Contact</button>
        </form>
    </div>

</body>
</html>